name = "username/moonbit-gltf-tools"

version = "0.1.0"

readme = "README.mbt.md"

repository = ""

license = "Apache-2.0"

keywords = [ "gltf", "glb", "3d", "mesh", "asset", "parser", "validation" ]

description = "A MoonBit library for reading, validating, and processing glTF/GLB 3D asset files. Provides node tree traversal, mesh/material/animation metadata extraction, resource dependency checking, and optimization reporting."

import {
  "moonbitlang/x@0.4.40",
}
