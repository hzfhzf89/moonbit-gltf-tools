# MoonBit国产基础软件开源生态大赛 - 项目申报书

## 一、 项目基本信息

- **项目名称**：`moonbit-gltf-tools` (MoonBit glTF 美术资产解析与优化工具箱)
- **选题方向**：MoonBit 基础库与系统工具生态建设
- **申报人/唯一贡献者**：hzfhzf89
- **开源许可证**：Apache License 2.0
- **项目仓库链接**：
  - **GitLink 仓库**：[Hzf78909/moonbit-gltf-tools](https://gitlink.org.cn/Hzf78909/moonbit-gltf-tools)
  - **GitHub 仓库**：[hzfhzf89/moonbit-gltf-tools](https://github.com/hzfhzf89/moonbit-gltf-tools)

---

## 二、 项目立项背景与设计目标

### 1. 立项背景
在现代 3D 游戏开发、虚拟现实（VR/AR）和 Web3D 应用中，**glTF 2.0 (GL Transmission Format)** 已成为跨平台的 3D 资产资产交换“黄金标准”。然而，在 3D 引擎加载和运行美术资产时，经常会遇到以下痛点：
1. **模型崩损与非法数据**：美术导出的 glTF 文件可能存在节点环路引用、索引越界或资源缺失，导致运行时加载直接崩溃。
2. **性能隐患**：未进行索引优化的网格（Unindexed Primitives）、过高的 Draw Calls 等会导致严重的渲染卡顿。
3. **安全隐患**：传统的 C/C++ 模型解析器存在内存越界风险，而 JavaScript/Python 等动态类型语言在大模型解析时性能堪忧，难以在工具链中进行自动化高强度校验。

### 2. 设计目标
`moonbit-gltf-tools` 旨在利用 **MoonBit** 强类型、高性能、零安全隐患的优势，打造一个专为 glTF 2.0 / GLB 资产设计的**轻量解析、格式校验与优化诊断引擎**。
- **不做渲染器**，专注于高性能的数据结构解析和工具链支持。
- **提供高合规性校验**，在资产编译/导入阶段为引擎守好安全第一关。
- **提供深入的资产优化诊断**，辅助美术流水线自动识别低效资产。

---

## 三、 核心功能与技术方案

### 1. 核心功能模块
- **GLB 二进制解包**：读取 GLB 二进制容器头信息，校验魔数与版本，安全拆分 JSON 数据块与二进制 BIN 数据块。
- **glTF 2.0 规格解析**：遵循 Khronos 官方 glTF 2.0 规范，解析场景、节点树、网格、材质、纹理、图像、采样器、访问器及骨骼蒙皮等核心数据。
- **节点树 DFS 遍历**：遍历场景图结构，计算节点深度，收集骨骼蒙皮节点与普通网格节点分布。
- **资源依赖追踪**：分析并生成资源清单，追踪外部 URI 资源和内嵌 base64 数据的依赖分布及总字节大小。
- **格式合规校验器**：校验节点层次合法性（检测环路引用）、材质纹理引用一致性及缓冲视口越界行为。
- **美术优化分析引擎**：统计顶点与 Draw Calls 数量；找出未进行顶点共享的网格；检测未使用的冗余材质、纹理和图像，输出调优诊断报告。

### 2. 技术路线与架构设计
- **语法适配**：基于最新的 MoonBit `v0.10.3` 规范编写，移除全部遗留/废弃语法，符合 `moon fmt` 和 `moon info` 严苛的格式化与公共 API 规范。
- **纯粹自治实现**：解析核心逻辑采用强类型匹配（如 `Json::Object` 等显式声明），不含任何外部不安全依赖。
- **多平台交叉部署**：提供 Wasm 与 Native 交叉编译适配。利用 `moonbitlang/x/fs` 的优秀 Native 适配特性构建 CLI 解析诊断工具，可无缝嵌入 CI/CD 自动化工作流。

---

## 四、 预期产出与社会价值

### 1. 预期产出
- 一个经过完整 snapshot 单元测试覆盖的高质量纯 MoonBit `gltf` 库。
- 一个可在本地独立编译、运行的 Native/Wasm 双目标 CLI 工具，支持直接对 glTF/GLB 文件进行深入体检。
- 完整的三平台 CI/CD 自动化校验流（GitHub Actions）。

### 2. 社会价值与生态意义
`moonbit-gltf-tools` 填补了 MoonBit 生态在 3D 资产底层解析与美术管线工具领域的空白。本项目的成功立项与开源，不仅展示了 MoonBit 在系统级与应用层工具开发中的独特类型安全魅力，更能够直接对接国产 3D/游戏引擎生态（如 Cocos, Laya 等），为元宇宙、数字孪生及国产基础软件工具链的建设提供强有力的底层组件支撑。
